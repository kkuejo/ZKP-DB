# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
*.egg-info/
dist/
build/

# Node.js
node_modules/
npm-debug.log
yarn-error.log
package-lock.json

# Large / generated artifacts (do not commit)
encrypted_package.zip

# Secrets / env files
**/.env.production
backend/secret_contexts/*.bin

# データファイル
data/*.json
data/*.csv
data/*.pkl

# 暗号鍵（大きなセットアップファイルのみ除外）
keys/*.ptau
keys/*.pkl

# 証明ファイル（一時的な証明ファイルを除外）
proofs/*.json
proofs/*.wtns

# Circomビルド成果物（一部を除外）
circuits/**/*.r1cs
circuits/**/*.sym
circuits/**/*.wtns
circuits/**/witness.wtns

# ZKP機能に必要なファイルは含める（上記のルールより優先）
# - keys/data_verification_0000.zkey
# - keys/verification_key.json
# - circuits/build/data_verification_js/*.wasm
# - circuits/build/data_verification_js/*.js

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# ログ
*.log

# 一時ファイル
tmp/
temp/

# PDF生成用のローカルフォント（自動ダウンロード）
scripts/fonts/
